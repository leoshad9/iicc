import React from 'react';
import { PlaceholderPage } from '../../shared/ui/PlaceholderPage';

export const BoardRoomPage: React.FC = () => (
  <PlaceholderPage title="Board Room" subtitle="Executive Board Room — intimate meeting space." />
);